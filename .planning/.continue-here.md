---
context: default
phase: 15-cpu
task: 3
total_tasks: 5
status: in_progress
last_updated: 2026-05-29T06:27:11Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Stepper index offset — Our Stepper initializes step 0 active in NewStepper(); the reference does not. Step() call 1 lands on stepper[1] in our CPU, not stepper[0]. All stepper indices in cpu/cpu.go are shifted +1 mod 6 vs the reference. Do NOT copy-paste stepper index numbers from simple-computer/simple-computer/cpu/cpu.go without applying the mapping: ref[N] → ours[(N+1)%6].

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Direct reference copy | Copying stepper index numbers from simple-computer/simple-computer/cpu/cpu.go verbatim | blocking | Apply +1 mod 6 shift to every stepper.GetOutputWire(N) call |

<current_state>
Phase 15 (CPU) is in progress. cpu/cpu.go is fully written and cpu/cpu_test.go has one passing test (TestDATALoadsImmediateIntoRegister). Files are uncommitted. Next: add remaining tests per Done Criteria, then commit.
</current_state>

<completed_work>

- Task 1: Tracer bullet test (TestDATALoadsImmediateIntoRegister) — DONE, passes
- Task 2: Full CPU implementation (cpu/cpu.go) — DONE, stepper index fix applied
</completed_work>

<remaining_work>

- Task 3: Add tests for LOAD, STORE, ADD, JMP, JMPZ (taken + not taken), CLF, IAR increment — one at a time, TDD style
- Task 4: Write blog/BLOG-15.md with notes from PHASE-15-cpu.md
- Task 5: Commit `phase-15: CPU`
</remaining_work>

<decisions_made>

- Stepper indices shifted +1 (mod 6): ref stepper[0]→ours[1], ref[1]→ours[2], ref[2]→ours[3], ref[3]→ours[4], ref[4]→ours[5], ref[5]→ours[0]
- `sio "simple-computer/io"` alias used to avoid stdlib io collision
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `cpu/cpu.go` — current implementation with corrected stepper indices
2. `cpu/cpu_test.go` — helpers already written (newTestCPU, writeMem, setReg, runInstr)
3. `simple-computer/simple-computer/cpu/cpu_test.go` — reference tests to adapt (same logic, different helper names)
4. `PRD-15-cpu.md` — Done Criteria and test table

## Infrastructure State
- `go test ./cpu/...` — 1 test passing (DATA), rest not yet written

<context>
The CPU implementation is a port of simple-computer/simple-computer/cpu/cpu.go with two differences: (1) import paths use our module (simple-computer/...) and (2) all stepper.GetOutputWire indices are shifted +1 mod 6 because our Stepper bootstraps step 0 active while the reference does not. The tracer bullet (DATA) passes. All remaining tests follow the same pattern as the reference test file.
</context>

<next_action>
Start with: Add TestLOADReadsMemoryIntoRegister in cpu/cpu_test.go. Write 0xBEEF to RAM[0x0600], then DATA R1,0x0600 + LOAD R1,R2, run 2×runInstr(c), check gpReg2.Value()==0xBEEF. Run go test ./cpu/... to confirm GREEN, then proceed to STORE test.
</next_action>
